;;; -*- mode: emacs-lisp; lexical-binding: nil -*-
;;
;; The most basic settings for the whole Emacs. It was originally a place for
;; handling platform differences, but I gave up on multi-platform setups.
;;
(require 'cl-lib)
(require 'dash)
(require 's)
(require 'f)






(provide 'my-system-config)
